Dev functions are always loaded (script and interactive shell)

Type testing :
	isnumber : if ARG is a number return 0 else return 1
	isuint ARG : if ARG is uint return 0 else return 1
	isint ARG  : if ARG is int return 0 else return 1
	isufloat ARG : if ARG is ufloat return 0 else return 1
	isfloat ARG : if ARG is float return 0 else return 1
	isuint ARG : if ARG is uint return 0 else return 1
	isuint ARG : if ARG is uint return 0 else return 1
	isuint ARG : if ARG is uint return 0 else return 1

Array :
	finditem ARG [*ARGS] : if ARG is in *ARGS return 0 else return 1

Misc :
    getkey :
        capture a single keyboard key including special keys,
        if raw key print raw,
        if special key print key name.

Zcurses :
    zz.dev.curses.inputbox [LENGTH] :
        open an inputbox, return the user input.
        LENGHT is the length of the input box, default is 20.
        Can be used with command substitution like this :
        user_input=$(zz.dev.curses.inputbox)
                                
